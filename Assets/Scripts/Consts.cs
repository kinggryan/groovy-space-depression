using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class GameStrings
{
    // Start is called before the first frame update
    public const string layerNPC = "NPCDialogue";
}
